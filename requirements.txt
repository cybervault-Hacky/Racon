# RACON — runtime dependencies (Termux / Android compatible)
# All packages here are pure-Python or provide pre-built wheels for ARM64.
# Heavy native-build packages (lxml, reportlab, cryptography) have been
# moved to requirements-optional.txt so RACON installs cleanly on Termux.

rich>=13.0
requests>=2.31
beautifulsoup4>=4.12
dnspython>=2.4
python-whois>=0.8
python-nmap>=0.7
geoip2>=4.7
tldextract>=5.0
jinja2>=3.1
ruamel.yaml>=0.18.0
